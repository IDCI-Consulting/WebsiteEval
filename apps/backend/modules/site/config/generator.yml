generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Site
    theme:                 admin
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          site
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:  ~
      list:
        display: [id, url, owner, creator, _siteType, created_at]
      filter:  ~
      form:    ~
      edit:    ~
      new:     ~
